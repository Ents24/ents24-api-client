{
  "operations": {

    "ClientRequestAccessToken": {
      "httpMethod": "POST",
      "uri": "auth/token",
      "summary": "Request an access token to authenticate future client requests",
      "parameters": {
        "client_id": {
          "description": "Your client ID obtained during registration.",
          "type": "string",
          "location": "postField",
          "required": true
        },
        "client_secret": {
          "description": "Your client secret obtained during registration.",
          "type": "string",
          "location": "postField",
          "required": true
        }
      }
    },

    "UserRequestAccessToken": {
      "httpMethod": "POST",
      "uri": "auth/login",
      "summary": "Request an access token to authenticate future client requests.",
      "parameters": {
        "client_id": {
          "description": "Your client ID obtained during registration.",
          "type": "string",
          "location": "postField",
          "required": true
        },
        "client_secret": {
          "description": "Your client secret obtained during registration.",
          "type": "string",
          "location": "postField",
          "required": true
        },
        "username": {
          "description": "The username of the account you request access to.",
          "type": "string",
          "location": "postField",
          "required": true
        },
        "password": {
          "description": "The password of the account you request access to.",
          "type": "string",
          "location": "postField",
          "required": true
        }
      }
    }
  }
}
