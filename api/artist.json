{
  "operations": {

    "ListArtists": {
      "httpMethod": "GET",
      "uri": "artist/list",
      "summary": "Multiple artist objects with selected fields.",
      "parameters": {
      }
    },

    "GetArtist": {
      "httpMethod": "GET",
      "uri": "artist/read",
      "summary": "Fetch data about a single artist",
      "parameters": {
        "id": {
          "location": "query",
          "description": "Unique identifier string of the Artist you want full details for",
          "required": true
        },
        "incl_events": {
          "location": "query",
          "description": "Decides whether or not a list of events the artists is performing at is included in the response.",
          "required": false
        },
        "incl_images": {
          "location": "query",
          "description": "Decides whether or not an artist images are included in the response.",
          "required": false
        },
        "full_description": {
          "location": "query",
          "description": "Decides whether full or summarised description text is included in the response.",
          "required": false
        }
      }
    },

    "GetArtistImage": {
      "httpMethod": "GET",
      "uri": "artist/image",
      "summary": "An artist image retrieved as either a JSON object or JPEG image.",
      "parameters": {
        "id": {
          "location": "query",
          "description": "Unique identifier string of the Event you want an image for.",
          "required": true
        },
        "size": {
          "location": "query",
          "description": "Size of image you want",
          "required": false,
          "enum": [
            "small",
            "medium",
            "large"
          ]
        },
        "format": {
          "location": "query",
          "description": "The format of the response you want back from the resource",
          "required": false,
          "enum": [
            "jpeg",
            "json"
          ]
        }
      }
    }

  }
}
